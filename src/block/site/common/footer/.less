footer {
	
	.footer__container {
		border-top:1px rgba(255,0,0,0.5) solid;
		padding:30px 0;
		display:flex;
		flex-direction:row;
		align-items:stretch;
		justify-content:space-between;
		
		.contact-item {
			display:flex;
			flex-direction:row;
			justify-content:flex-end;
			align-items:stretch;
		}
	}
	
	.__padding__left {
		padding-left:45px;
		
		a, span {
			#site.color(#000000);
			#site.bold(700);
			#site.size(14px);
			#site.ff('Open Sans');
		}
		
		.__semi-black {
			#site.color(#767676);
			#site.bold(400);
		}
	}
	
}